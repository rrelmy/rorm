language: php
php:
  - '7.1'
  - nightly

sudo: false

services:
  - mysql

install:
 - composer install

before_install:
  - mysql -e 'CREATE DATABASE rorm;'

script: phpunit --coverage-clover=coverage.clover

after_script:
  - wget https://scrutinizer-ci.com/ocular.phar
  - php ocular.phar code-coverage:upload --format=php-clover coverage.clover
